// if we have a thousand exports to send we put all of them here. just so we don't clutter the pages component files
export { default as SideNav } from './SideNav';
export { default as Header } from './Header';
export { default as Footer } from './Footer';